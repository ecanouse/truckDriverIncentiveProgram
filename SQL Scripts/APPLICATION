CREATE TABLE `new_schema`.`APPLICATION` (
  `applicationsID` INT NOT NULL AUTO_INCREMENT,
  `uID` INT NOT NULL,
  `sponsorID` INT NULL,
  `status` INT NULL,
  `description` VARCHAR(100) NOT NULL,
  `date` DATE NOT NULL,
  PRIMARY KEY (`applicationsID`));
